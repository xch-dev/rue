#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub enum TokenKind {
    Ident,
    Int,
    String { is_terminated: bool },
    OpenParen,
    CloseParen,
    OpenBrace,
    CloseBrace,
    Fun,
    If,
    Else,
    Nil,
    True,
    False,
    Comma,
    Colon,
    Arrow,
    Plus,
    Minus,
    Star,
    Slash,
    Percent,
    LessThan,
    GreaterThan,
    LessThanEquals,
    GreaterThanEquals,
    Equals,
    NotEquals,
    Whitespace,
    LineComment,
    BlockComment { is_terminated: bool },
    Unknown,
}
