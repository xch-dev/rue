type Any = Atom | Pair<Any, Any>;

subtype Atom(value: Any) if !listp(value);

subtype Pair<F = first, R = rest>(value: Any) if listp(value) {
    first: first(value),
    rest: rest(value),
}

fn main() -> Atom {
    "Hello, world!"
}
